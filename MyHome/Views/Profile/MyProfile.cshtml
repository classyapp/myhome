@model Classy.DotNet.Responses.ProfileView

@{
    ViewBag.Title = "AgentProfile";
}

@Model.Id

@if (Model.IsProfessional)
{
    @Model.ProfessionalInfo.CompanyContactInfo.Name
    @Html.RouteLink("Request Review", "RequestReview")
}
else
{
    @Model.ContactInfo.Name
}